crear un proyecto en firebase console

//solo la primera vez
npm install -g firebase-tools
firebase login
//en la carpeta del proyecto
firebase init
//una vez que se haya movido todos los archivos a la carpeta public
firebase deploy